<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- GŁÓWNY MOTYW APLIKACJI -->
    <style name="Theme.YourSoundScape" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status bar / navigation bar możesz dodać później -->
        <!-- <item name="android:statusBarColor">@android:color/transparent</item> -->
        <!-- <item name="android:navigationBarColor">@android:color/transparent</item> -->
    </style>

    <!-- Karta notatki -->
    <style name="Shape.NoteCard" parent="ShapeAppearance.Material3.MediumComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>

    <!-- Miniatura zdjęcia -->
    <style name="Shape.NoteImage" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

</resources>
